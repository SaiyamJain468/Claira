[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "claira"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
app = "app:main"
